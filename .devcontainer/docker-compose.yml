services:
  app:
    build:
      # 1. Change context to the parent folder (project root)
      context: ..
      # 2. Specify the Dockerfile location relative to the new context
      dockerfile: .devcontainer/Dockerfile

    # ... the rest of the file is unchanged ...
    volumes:
      - ..:/workspaces/rsvp-priority:cached
    command: sleep infinity